name: lint, run unit tests, run integration tests

on:
  pull_request:
    branches: [ "main" ]

  # make workflow "callable" by others
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest

    container: python:3.12

    steps:
    - name: checkout
      uses: actions/checkout@v7

    - name: install uv
      uses: astral-sh/setup-uv@v8.3.2

    - name: uv sync
      run: uv sync --frozen

    - name: lint using ruff
      run: uv run ruff check --output-format github ./webapp ./tests ./migrations

  test-unit:
    runs-on: ubuntu-latest

    container: python:3.12

    env:
      TZ: Europe/Berlin

    steps:
    - name: checkout
      uses: actions/checkout@v7

    - name: install uv
      uses: astral-sh/setup-uv@v8.3.2

    - name: uv sync
      run: uv sync --frozen

    - name: run pytest
      run: uv run python -m pytest tests/unit

  test-integration:
    runs-on: ubuntu-latest

    container: python:3.12

    env:
      TZ: Europe/Berlin

    services:
      postgresql:
        image: postgis/postgis:15-3.5-alpine
        env:
          POSTGRES_USER: park-api
          POSTGRES_PASSWORD: development
          POSTGRES_DB: park-api
          PGUSER: park-api
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: checkout
      uses: actions/checkout@v7

    - name: install uv
      uses: astral-sh/setup-uv@v8.3.2

    - name: uv sync
      run: uv sync --frozen

    - name: run pytest
      run: uv run python -m pytest tests/integration
